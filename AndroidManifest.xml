<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.xvshu.android" >


  <application
          android:allowBackup="true"
          android:icon="@drawable/icon"
          android:label="@string/app_name"
          android:theme="@android:style/Theme.Light.NoTitleBar" >
    <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
            android:name=".HelloActivity"
            android:label="@string/app_name" >
    </activity>
    <activity
            android:name=".ListViewDiarysActivity"
            android:label="@string/app_name" >
    </activity>

    <activity
            android:name=".CalcActivity"
            android:label="@string/app_name" >
    </activity>

    <activity
            android:name=".CalcHistoryActivity"
            android:label="@string/app_name" >
    </activity>

    <activity
            android:name=".WebMainActivity"
            android:label="@string/app_name" >
    </activity>

    <activity
            android:name=".WebRequestActivity"
            android:label="@string/app_name" >
    </activity>

    <service android:name=".service.HelloService" />

  </application>
  <uses-sdk android:minSdkVersion="7"  android:targetSdkVersion="24" />
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


</manifest>

